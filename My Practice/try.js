[10, 20, 5, 14, 6].map(num => console.log(num ** 2))

function hame() {
    const hint = Math.random() > 0.5 ? "speed" : "color"
    const naje = {
        speed : 24,
        color : "orange"
    }
    console.log(naje[hint]);
}
hame()

["tunde", "ade", "bola", "tayo", "ola"].map(egg => console.log(egg))